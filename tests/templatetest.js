/* const { test } = require('@playwright/test')
require('./static/authentication')
const {} = require('../src/pages/')
const {} = require('./static/testValues')

test.describe('', () => {
  test('', async ({ page }) => {
    const namePage = new nameTestPage(page)
  })
}) */
